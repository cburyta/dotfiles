# fix problem when reattaching to a session, we loose our colors
# this seems like it indicates an env var problem rooted elsewhere, and this
# feels like it's simply fixing the symptom
# alias tmux="TERM=screen-256color-bce tmux"
